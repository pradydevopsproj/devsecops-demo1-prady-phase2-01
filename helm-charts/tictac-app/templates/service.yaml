apiVersion: v1
kind: Service
metadata:
  name: {{ include "tictac-app.fullname" . }}
  labels:
    {{- include "tictac-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tictac-app.selectorLabels" . | nindent 4 }}
